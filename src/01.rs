fn main() {  // main() runs first in every executable rust program
    // ^ nothing between these parenthases bc we have no parameters
    println!("Hello, world!");
    // with an ! because we're calling a macro instead of the function
}

// Compile this by running `rustc main.rs` in a terminal
// then run the program with `./main`
